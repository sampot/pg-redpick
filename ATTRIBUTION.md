# Attribution

This game uses **CSS／DOM card faces**, original Web Audio synthesis, and system／web-safe fonts only.

No third-party art, audio, or font packs are bundled.

| Asset | Notes |
| --- | --- |
| Cards／table | Styled in `styles.css`；ranks／suits drawn as text |
| SFX | Procedural tones in `audio.js`（Web Audio API） |
| Fonts | Browser stacks（serif／sans）；no webfont files |

If third-party packs are added later, list author, license, and source URL here（project convention: credit even when the license does not require it）.
